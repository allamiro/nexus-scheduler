# STIG Manager + Keycloak + MCP server lab
#
# LOCAL DEVELOPMENT LAB — plain HTTP, Keycloak in start-dev mode.
# See README.md "Production and air-gap notes" before using any of this
# outside a lab.
#
# Host ports (chosen to coexist with the main nexus-scheduler compose
# stack on the same machine):
#   Keycloak     -> localhost:8180
#   STIG Manager -> localhost:54000
#   MCP server   -> localhost:8005  (streamable-HTTP endpoint at /mcp)

name: stigman-mcp-lab

networks:
  stigman-lab:
    driver: bridge

volumes:
  keycloak-db-data:
  stigman-db-data:

services:
  keycloak-db:
    image: postgres:16.14-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${KEYCLOAK_DB_NAME:-keycloak}
      POSTGRES_USER: ${KEYCLOAK_DB_USER:-keycloak}
      POSTGRES_PASSWORD: ${KEYCLOAK_DB_PASSWORD:?set in .env}
    volumes:
      - keycloak-db-data:/var/lib/postgresql/data
    networks: [stigman-lab]
    # No published ports: only Keycloak needs to reach this database.
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 12

  keycloak:
    image: quay.io/keycloak/keycloak:26.6.4
    restart: unless-stopped
    command: ["start-dev", "--import-realm"]   # start-dev: LAB ONLY (HTTP, no clustering)
    depends_on:
      keycloak-db:
        condition: service_healthy
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:?set in .env}
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak-db:5432/${KEYCLOAK_DB_NAME:-keycloak}
      KC_DB_USERNAME: ${KEYCLOAK_DB_USER:-keycloak}
      KC_DB_PASSWORD: ${KEYCLOAK_DB_PASSWORD:?set in .env}
      KC_HEALTH_ENABLED: "true"
      # No KC_HOSTNAME on purpose: the issuer follows the request host, so
      # browsers get http://localhost:8180/realms/stigman and containers
      # get http://keycloak:8080/realms/stigman. See README "Keycloak URLs".
      # Values substituted into keycloak/realm-import/stigman-realm.json:
      NEXUS_MCP_CLIENT_SECRET: ${NEXUS_MCP_CLIENT_SECRET:?set in .env}
      STIGMAN_ADMIN_PASSWORD: ${STIGMAN_ADMIN_PASSWORD:?set in .env}
      # Used in the web client's redirect URIs inside the realm import,
      # so the whole lab moves ports from .env alone:
      STIGMAN_HOST_PORT: ${STIGMAN_HOST_PORT:-54000}
    volumes:
      - ./keycloak/realm-import:/opt/keycloak/data/import:ro
    networks: [stigman-lab]
    ports:
      - "${KEYCLOAK_HOST_PORT:-8180}:8080"
    healthcheck:
      # ubi-micro image has no curl/wget; use bash /dev/tcp against the
      # management interface (port 9000, KC_HEALTH_ENABLED).
      test:
        - CMD-SHELL
        - "exec 3<>/dev/tcp/127.0.0.1/9000 && echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && grep -q '\"status\": \"UP\"' <&3"
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s

  stigman-db:
    image: mysql:8.0.46
    restart: unless-stopped
    command:
      - mysqld
      - --innodb-buffer-pool-size=512M
      - --sort_buffer_size=32M
    environment:
      MYSQL_DATABASE: ${STIGMAN_DB_NAME:-stigman}
      MYSQL_USER: ${STIGMAN_DB_USER:-stigman}
      MYSQL_PASSWORD: ${STIGMAN_DB_PASSWORD:?set in .env}
      MYSQL_RANDOM_ROOT_PASSWORD: "yes"
    volumes:
      - stigman-db-data:/var/lib/mysql
    networks: [stigman-lab]
    # No published ports: only STIG Manager needs to reach this database.
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u$${MYSQL_USER} -p$${MYSQL_PASSWORD} --silent"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s

  stigman:
    image: nuwcdivnpt/stig-manager:1.5.9
    restart: unless-stopped
    depends_on:
      stigman-db:
        condition: service_healthy
      keycloak:
        condition: service_healthy
    environment:
      STIGMAN_API_PORT: "54000"
      STIGMAN_DB_HOST: stigman-db
      STIGMAN_DB_PORT: "3306"
      STIGMAN_DB_SCHEMA: ${STIGMAN_DB_NAME:-stigman}
      STIGMAN_DB_USER: ${STIGMAN_DB_USER:-stigman}
      STIGMAN_DB_PASSWORD: ${STIGMAN_DB_PASSWORD:?set in .env}
      # Internal (container-to-container) issuer/JWKS source:
      STIGMAN_OIDC_PROVIDER: http://keycloak:8080/realms/stigman
      # Browser-facing issuer used by the web client:
      STIGMAN_CLIENT_OIDC_PROVIDER: http://localhost:${KEYCLOAK_HOST_PORT:-8180}/realms/stigman
      STIGMAN_CLIENT_ID: stig-manager
      # Enforce audience validation (tokens carry aud=stig-manager via a
      # Keycloak audience mapper; see realm import).
      STIGMAN_JWT_AUD_VALUE: stig-manager
      STIGMAN_CLASSIFICATION: U
    networks: [stigman-lab]
    ports:
      - "${STIGMAN_HOST_PORT:-54000}:54000"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:54000/api/op/configuration || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s

  stigman-mcp:
    build: ./server
    image: stigman-mcp:1.0.0
    restart: unless-stopped
    depends_on:
      stigman:
        condition: service_healthy
      keycloak:
        condition: service_healthy
    environment:
      STIGMAN_API_URL: http://stigman:54000/api
      KEYCLOAK_TOKEN_URL: http://keycloak:8080/realms/stigman/protocol/openid-connect/token
      OIDC_CLIENT_ID: ${NEXUS_MCP_CLIENT_ID:-nexus-mcp}
      OIDC_CLIENT_SECRET: ${NEXUS_MCP_CLIENT_SECRET:?set in .env}
      MCP_PORT: "8005"
    networks: [stigman-lab]
    # Published so LibreChat (running in the separate nexus-scheduler
    # compose stack) can reach it via host.docker.internal:8005 — see
    # README "Wire LibreChat to the MCP server" for the alternatives.
    ports:
      - "${MCP_HOST_PORT:-8005}:8005"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import socket; socket.create_connection(('127.0.0.1', 8005), 3)\""]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
