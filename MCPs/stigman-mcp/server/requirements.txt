# Resolved and frozen inside the Docker image; for air-gap the image
# itself (docker save/load) is the transfer artifact, so exact pins
# live in the built image rather than here.
mcp>=1.9,<2
httpx>=0.27,<1
