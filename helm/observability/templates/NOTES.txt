{{ .Chart.Name }} deployed.

Grafana:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "observability.fullname" . }}-grafana {{ .Values.grafana.service.port }}:{{ .Values.grafana.service.port }}
  user: {{ .Values.grafana.adminUser }}
  pass: kubectl -n {{ .Release.Namespace }} get secret {{ include "observability.fullname" . }}-grafana-admin -o jsonpath='{.data.password}' | base64 -d

App scraping: any pod annotated prometheus.io/scrape=true is collected
automatically{{ if .Values.alloy.appNamespace }} in namespace {{ .Values.alloy.appNamespace }}{{ end }} — the nexus-scheduler app chart sets those
annotations on the api and worker already. Do NOT also enable the app
chart's observability.serviceMonitor unless a separate Prometheus
Operator owns those scrapes; double collection skews rate() panels.

Scope reminder: single-replica, filesystem-backed. For clusters that
already run a monitoring stack, use the app chart's serviceMonitor
values instead of this chart.
